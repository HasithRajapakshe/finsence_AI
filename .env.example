# Root-level reference only — each service has its own .env (copy each
# service's .env.example to .env before running). This file documents the
# shared values so they stay consistent across services.
DATABASE_URL=postgresql://finsense:finsense@postgres:5432/finsense
REDIS_URL=redis://redis:6379/0
QDRANT_URL=http://qdrant:6333
